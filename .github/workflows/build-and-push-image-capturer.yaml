name: Build and publish image-capturer service image

on:
  push:
    branches:
      - master
    paths:
      - "*"
      - "shared/**"
      - "image-capturer-service/**"
  workflow_dispatch:

jobs:
  push_to_registry:
    name: Build
    runs-on: ubuntu-24.04-arm
    permissions:
      packages: write
      contents: read
      id-token: write
    steps:
      - name: Check out the repo
        uses: actions/checkout@v5
      - name: Build image-capturer service
        uses: ./.github/actions/build-service
        with:
          service-name: image-capturer
          dockerhub-username: ${{ secrets.DOCKER_USERNAME }}
          dockerhub-password: ${{ secrets.DOCKER_PASSWORD }}
