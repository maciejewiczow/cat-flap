name: Build and publish image-saver service image

on:
  push:
    branches:
      - master
    paths:
      - "*"
      - "shared/**"
      - "image-saver-service/**"
  workflow_dispatch:

jobs:
  push_to_registry:
    name: Build
    runs-on: ubuntu-24.04-arm
    permissions:
      packages: write
      contents: read
      id-token: write
    steps:
      - name: Build image-saver service
        uses: ./.github/actions/build-service
        with:
          stervice-name: image-saver-service
          dockerhub-username: ${{ secrets.DOCKER_USERNAME }}
          dockerhub-password: ${{ secrets.DOCKER_PASSWORD }}
